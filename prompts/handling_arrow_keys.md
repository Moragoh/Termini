# Feature Request

Currently, the termini terminal only takes in keyboard inputs. Say there is a CLI tool that takes input by arrow keys to navigate through a menu and the user has to press enter to choose that option. The current implementation of Termini cannot handle that. It can only handle inputs through the keyboard, not arrow keys.

Make it so termini can handle arrow key inputs so that navigating CLI tools like the one described above can work in the termini terminal